using System;
using System.Collections.Generic;
using System.Linq;
using System.Threading.Tasks;
using DBSAPI.Data;
using System.Linq;

namespace DBSAPI.Model
{
    public class CustomerBookingView
    {
        private readonly CustomerContext db;

        
//       var res=new CustomerContext();
//        using (var db = private new CustomerContext())
//            {    
//                    var students = from s in ctx.Students
//                                   orderby s.StudentName ascending
//                                   select s;
//            }

////BookingDetail bookingDetail = new BookingDetail();

//// List<BookView> bk = new List<BookView>();
//var res =from c in db.Customers join  bd in


//                 //var res = from cust in db.Customers
//                 //          join so in db.SalesOrders
//                 //            on cust.Id equals so.CustomerId
//                 //            into MatchedOrders
//                 //          from mo in MatchedOrders.DefaultIfEmpty()
//                 //          select new { cust.CustomerId, mo.SalesOrderId };

    }
}
